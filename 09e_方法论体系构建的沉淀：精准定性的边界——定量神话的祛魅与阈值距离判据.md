<!--
SPDX-License-Identifier: CC-BY-SA-4.0
Copyright (c) 2026 wUwproject
Licensed under Creative Commons Attribution-ShareAlike 4.0 International (CC BY-SA 4.0).
See https://creativecommons.org/licenses/by-sa/4.0/ for details.
-->

# 方法论体系构建的沉淀：精准定性的边界——定量神话的祛魅与阈值距离判据

> 我们被一个神话包围：只有定量才是精准的，一切都要量化才配叫严谨。但这个神话经不起一次实测推敲——我自己的结构化写作引擎在 3.0.0b11 到 3.1.0b5 的三十多次迭代里，反复撞见同一个事实：KV 缓存从 FP16 换成 Q8_0，显存省了 412MB，写出来的章节定性相同；llama.cpp 直挂 8 t/s 换到 LM Studio 20+ t/s，吞吐翻了两倍半，生成的文本同样过六检；Qwen3-8B 判不了的抽象标准，换更大的模型才判得了——定性能力的瓶颈从来不在小数点后第几位，而在状态空间本身划得够不够准。这篇文章把这条线拉直：定性不是没有精度，而是另一种坐标——离散的坐标；追求定量精度的价值不取决于精度本身，而取决于两件事：离决策阈值的距离，和误差传播路径的长度。离阈值远、路径短，再高的精度也是空耗；离阈值近、路径长，每一分精度都是救命钱。两道杠的深浅、Q8_0 与 FP16 的差异、"必须语义拆分"的指令，全都在这两个判据下各归其位。
>
> 更新：2026-08-18（09e：精准定性的边界篇。承接 09 的"穷举宿命"、09b 的"精密度优先于正确度"、09c 的"语义匹配不对等"与 09d 的"颗粒度与对齐悖论"，把 structured-writer 引擎 3.0.0b11~3.1.0b5 三十余次迭代中反复出现的"定量精度空耗"现象提炼为双判据框架：决策阈值距离 × 误差传播路径长度。核心命题：定量神话是伪命题，精准定性是离散坐标的精准，定性精度的价值取决于阈值距离与传播路径长度。工程实证取自本人 structured-writer 引擎更新日志（3.0.0b11/b12/b13/b14/b21/b22、3.1.0b1/b4/b5）；文献佐证取自本地 KB（大模型万象/政经文哲/τ scaling/LLM奠基理论），均为实有原文，详见文末引用表）

---

## 一、引子：两道杠就是阳性——深浅有没有意义

我的结构化写作引擎有一个固定流程：规划章节，写作，跑六种检查（格式、连续性、承诺链、逻辑、语义、推理），不合格就修，修完重检，通过后进入下一章。这个流程本身不复杂——执行链不长，环节有限，每一步的输入输出都是确定的协议。真正让我反复停下来想问题的，是流程里那些"看起来是定量、实际上在空耗"的地方。

最典型的一次发生在 3.0.0b13。当时我在调 KV cache 的量化：同一份模型权重，KV 缓存用 FP16 存还是用 Q8_0 存，显存占用差多少？实测结果很干净——同一模型、同一上下文长度，FP16 的 KV 缓存比 Q8_0 多占 412MB，而两者生成的文本在语义上无法区分。Q8_0 是 8-bit 量化，FP16 是 16-bit 浮点，精度差了一整个数量级——但章节照样过六检，情节照样连贯，角色照样不出戏 [6]。

这就像验孕棒上的两道杠：深浅有差别，但阳性就是阳性。没人会因为"第二道杠比上次浅了一点"就把阳性改判成阴性——除非它浅到接近消失。而"接近消失"这个状态，恰恰是定性判断最尴尬的时刻：它既不是明确的阳性，也不是明确的阴性，而是落在阈值附近的灰色地带。**深浅不是没有意义，深浅的意义只在靠近阈值时才显现。**

这篇文章要处理的问题，就是"深浅"与"两道杠"之间的关系——定量精度与定性结论之间的关系。我把它拆成四个问题：

1. 定性是不是"没有精度的权宜之计"？还是说，定性本身就是一种坐标，只是离散的？
2. 为什么"你必须进行语义拆分"这类指令是死板废话？死板废话的结构性病因是什么？
3. 定性中到底藏不藏着定量？"两道杠的深浅"是不是被定性判断丢掉的定量信息？
4. 什么时候该为定量精度付费，什么时候该省下这笔钱？

四个问题一条主线：**精准不是定量的专利，定性有自己的精准形态；而定量的价值不取决于数字位数，取决于两个判据——离决策阈值的距离，和误差传播路径的长度。**

## 二、定量不是精准的唯一形态：定性是离散化坐标

先破一个最常见的误解：定性 = 模糊，定量 = 精准。这个等式在工程里几乎被当成公理用——"这个指标太主观了，能不能量化一下？""没有数据支撑的结论不严谨。" 但细究起来，这个等式偷换了概念：**它把"定量的精度"和"精准"混为一谈，好像精准只能以连续数值的形式存在。**

我自己的方法论体系里有一个现成的反例。09d 写过的五确定决策树：问题分级（HARD/SOFT）、原因清晰、位置清晰、修复方式清晰、执行者确定——这五个判断全是定性的：分级是二元的（阻断/不阻断），位置是"能定位/不能定位"，执行者是"代码/LLM"。它们一个数字都没有，但整棵决策树的确定性恰恰来自这五个定性判断，而不是来自任何定量指标 [5]。细碎处理能做对，靠的是"这一行就是 L02S01 缺了编号"这个定性断言，而不是"相似度 0.87"这个定量分数——事实上 09d 里我专门写过，正文偏离概述的相似度 0.55 和 0.6 之间差什么、改哪里能提升，模型根本不知道 [5]。

这指向一个更深的观察：**定性不是没有坐标，定性是离散化的坐标。**

把"精准"重新定义一下。一个判断要称得上精准，它至少要做到"不混淆"：两个不同的情形，不能被判断成同一个。这个要求跟数字无关——把世界分成 A/B 两类，只要每个实例都能无歧义地落到 A 或 B，这个二分就是精准的；分不清某个实例该归 A 还是 B，这个二分才是不精准的。数字只是坐标的一种编码方式，离散标签是另一种。**精准的对立面不是定性，是混淆。**

离散坐标有一个连续坐标没有的优势：它天然是确定的。连续坐标永远携带测量误差——你测得 0.5867，真实的可能是有理数也可能不是，任何有限位数的读数都是近似；而离散坐标一旦划界清晰，分类结果就是确定的——阳性就是阳性，不携带"±0.01"这种尾巴。09b 里我写过"精密度优先于正确度"：配置推动的穷举一致性，靠的是把推动点位统一，让偏差一致化从而互相抵消——那是定量世界的精密度策略 [4]。而离散坐标直接绕过了这个问题：没有连续的读数，就没有读数的误差，只有划界本身的误差。划界划得准，定性就精准；划界划得糊，定性就稀烂。**定性的精度问题，本质是划界问题，不是读数问题。**

但离散坐标也有它的问题——这正是用户原话里"混沌状态的因由"：**几种状态，是定性分类的量化，也是混沌状态的因由。**

这句话值得拆开。二元的状态空间（零和/正和/负和之外，最基础的是对/错、真/假、阳性/阴性）只有两个格点，任何一个连续量的投影都会在这个二值空间里产生不连续性：一个量从 0.49 滑到 0.51，在连续坐标里是微小的移动，在二元坐标里是从"错"跳到"对"的跃迁。跃迁边界上的一切都是混沌的——因为离散坐标丢失了"离边界有多远"这个信息。这就是为什么"两道杠的深浅"会成为一个问题：阳性/阴性是二元离散坐标，深浅是连续信息，离散坐标把深浅压缩掉了，于是靠近阈值的样本全部落入同一种"阳性"，而它们实际上离"阴性"只有一步之遥。

多元坐标缓解了这个混沌，但没有消除它：与/或的多元逻辑把状态空间从 2 格点扩到 n 格点，每个格点之间的边界仍然是跃迁。更糟糕的是，多元状态空间引入了"格点间距离"的问题——"与"和"或"之间隔了几步？离散坐标无法回答，因为离散坐标只有"相同/不同"，没有"相近/相远"。

于是我们被迫面对一个看似矛盾的结论：**定性是精准的（划界清晰时），也是混沌的（边界附近时）；定量的连续坐标是平滑的（永远有距离感），也是永远不精确的（任何读数都是近似）。** 两种坐标各有各的精度，也各有各的失明区。这不是"定量优于定性"或"定性优于定量"的二选一，而是——**什么时候用哪种坐标，取决于判断本身离决策阈值有多远。** 这就是第四节要展开的核心判据。

在展开之前，还有一个定性坐标的形态学问题要先处理：离散坐标不是只有"二元的对/错"这一种形态。定性坐标是一个谱系，从最粗到最细：

| 形态 | 格点数 | 例子 | 精度来源 |
|------|--------|------|----------|
| 二元 | 2 | 阳性/阴性、HARD/SOFT、零和/正和 | 划界是否无歧义 |
| 多元 | n | 与/或/非、七级量表、T0/T1/T2 | 格点间是否可区分 |
| 区间 | 连续但离散化 | 精确范围"30-60% 之间"、±15% 容差 | 边界是否可信 |
| 连续定量 | 无穷 | 0.5867、FP16 位宽 | 读数是否逼近真值 |

注意区间这一行：**"精确的范围"本身就是一种定性形态，而且是定性和定量之间的桥。** 用户原话里的"是需要精准的论断，还是精确的范围"——精准的论断是二元坐标（论断=对/错），精确的范围是区间坐标（范围=上下界）。"温度控制在 35-38 度之间"比"温度必须精准"高明得多：前者给出了可验证的边界，后者只给了无内容的强调。区间是定性坐标里精度最高、又最不需要虚假读数的一种形态——它承认不确定，但把不确定圈进了边界。这就是"精确的范围"为什么常常比"精准的论断"更精准：**论断用二元坐标强行压缩连续现实，范围用区间坐标诚实地标注不确定性。** 09d 的"字数 ±15% 容差"约束、structured-writer 修复引擎的 max_tokens 超窗降级（8192 窗口自动降到 7168 输出上限）[11]，都是精确范围的工程形态。

定性坐标的谱系还有一个工程推论：**给 LLM 的指令，能划区间的别划点，能划多元的别划二元，能划二元的别写废话。** 这不是风格偏好，是坐标形态的精度匹配——指令的坐标形态必须匹配任务的决策结构：任务是二元的（通过/不通过），就给它清晰的二元判据；任务的结论是一个范围（比如"保留原意、字数 ±15%"），就给它区间坐标；任务需要细分（修复分级 T0/T1/T2），就给它多元坐标。指令给错了坐标形态，再多的"必须"也救不回来。

## 三、死板废话的诊断：强调的语义密度

现在处理第二个问题：为什么"你必须进行语义拆分"这类指令是死板废话？

先给死板废话下一个结构性的定义。**死板废话 = 强调成分（语气词、祈使词、重复的"必须"）占比高，而语义成分（可操作、可验证、可枚举的内容）占比低的指令。** 它的特征是：读完一遍，你完全同意它是对的，但完全不知道下一步该做什么。

"你必须进行语义拆分"——谁都会同意语义拆分是好事，但这句话没有定义：拆分什么？拆到什么粒度？用什么方法拆？输出什么结构？判断拆得对的标准是什么？四个问题一个都没回答。这句话的语义密度几乎为零：它传达了态度（拆分很重要），没有传达操作（怎么拆）。而 LLM 是语义驱动的执行器——它不读语气，它读的是操作语义。一句没有操作语义的强调，对 LLM 而言等于没有指令：模型只能自己猜"语义拆分"是什么意思，猜的偏差就是执行偏差。

用户原话一针见血："'必须'是强调，但是强调的内容不明确。" 这就是死板废话的解剖学结构：**强调的语义密度为零——语气是满的，内容是空的。**

诊断死板废话，我给出三个可操作的判据，合称**指令侧三查**（与 09d 的问题侧四确定呼应，但落在指令上）：

**① 可枚举吗？** 指令的对象能否列出有限的集合？"拆分语义"——拆出哪几类？"检查格式"——检查哪几种格式？可枚举，指令才有落点；不可枚举，指令就是口号。

**② 可定位吗？** 指令的作用范围能否指出坐标？"对第三段进行重写"可以定位；"把这篇写得更好"不可定位。不可定位的指令，执行者只能全文盲扫。

**③ 可验证吗？** 执行完怎么知道做对了？"字数 ±15%"可验证；"写得自然一点"不可验证。不可验证的指令，执行者无法确认终点，只能靠感觉停。

三查全过，指令是合格的；三查全不过，指令就是死板废话——无论它叠多少个"必须"。

死板废话为什么是工程毒药？因为它有双重成本：**第一重，执行偏差**——LLM 用自己猜的操作语义执行，猜错就做错；**第二重，偏差不可诊断**——你发现结果不对，想追责，但指令本身没有可对照的规范，你无法区分是"模型没做好"还是"指令本来就空"。前者是执行问题，后者是指令问题，死板废话让两者永远无法区分——这就是为什么 09d 写的"验证迷思"会雪上加霜：带噪声的验证器验证带噪声的修复 [5]，而指令的模糊让"验证"连验证什么都说不清。

这里有一个重要的反直觉结论：**死板废话不产生于"要求严苛"，恰恰产生于"要求空洞"。** 用户原话里的"死板的废话来源于要求严苛的模糊语义"——严苛的是语气（必须、绝对、一定不能），模糊的是语义（到底做什么）。语气和语义的落差越大，指令越废。一个只有语气没有语义的指令，就像一把只有刀柄没有刀刃的刀：你握得再紧，也切不开任何东西。反过来说，语义明确的指令根本不需要语气词——"把 L02S01 的末行编号补上"这句话没有任何"必须"，但它是完美的指令：可枚举（一行）、可定位（L02S01 末行）、可验证（正则核对）。

工程实证在我自己的引擎里反复出现。3.0.0b22 那次修复，是我印象最深的一次：修复引擎在共享模型后开始崩溃，我当时的第一反应是往"内存/KV"方向排查——KV cache 膨胀、显存不够、内存不足——全是定量方向的排查。用户在日志里直接骂醒我："压根不是爆内存包缓存，查代码！！"[11]。这句话本身就是一个定性指令的教科书案例：它没有给我任何数字，但它把排查方向从"定量资源排查"精确地拉回"代码逻辑排查"——原因（并发调用同一 Llama 实例）、位置（run() 后台线程在锁外执行）、修复方式（防重入）全部被这一句话点明。**一条定性指令的效率，远超我几轮定量排查的总和。** 事后复盘，崩溃的真因确实是代码级的并发竞态（b22 防重入修复），跟内存、KV 毫无关系 [11]。如果当时我继续往定量方向查，可能还要空耗很多轮。

这个实证还揭示了死板废话的另一面：**不是只有人类会给 LLM 死板废话，LLM 的默认行为模式也会自发生产死板废话。** 当你问一个模型"这个 bug 怎么修"，如果问题本身定位模糊，模型会输出"建议检查内存使用情况，考虑优化 KV cache 分配"这类听起来专业、实际上什么也没定位的废话——因为它的训练数据里全是这种"专业而空洞"的回应。**死板废话是概率模型的默认输出模式：它最擅长的就是生成"语义密度接近零但语气完全正确"的文本。** 这也是为什么提示词工程的核心动作不是"加语气"而是"给坐标"——你给模型坐标，模型才能给结论；你只给语气，模型只能回敬语气。

## 四、定性中隐藏定量：决策阈值距离判据

第三个问题：定性中到底藏不藏着定量？"两道杠的深浅"是不是被定性判断丢掉的定量信息？

答案是：藏着，而且藏得很深。**任何二元的定性判断，背后都有一条连续的量纲和一道阈值。** 验孕棒的两道杠：背后的量纲是 hCG 浓度，阈值是检测试剂盒的灵敏度。阳性不是"没有深浅"，而是"深浅被阈值二值化了"。HARD/SOFT 问题分级：背后的量纲是问题的严重程度，阈值是"是否阻断流程"。语义相似度 0.55 vs 0.6：背后的量纲是向量距离，阈值是"是否判定为偏离概述"。

但这里的关键不是"定性中藏了定量"这个事实本身，而是它的推论：**被丢掉的深浅信息，其价值取决于样本离阈值的距离。**

把量纲画出来。设一个连续量 x，阈值在 x₀，定性判断就是 sign(x - x₀)：x > x₀ 判 A，x ≤ x₀ 判 B。对任意一个样本：

- 若 |x - x₀| 很大（远离阈值）：定性判断高度稳定。x 的微小扰动（测量误差、量化误差、采样噪声）根本不会改变 sign。**这时候深浅信息毫无价值——它是死信息。**
- 若 |x - x₀| 很小（贴近阈值）：定性判断极度脆弱。x 的微小扰动就可能翻转 sign。**这时候深浅信息是命脉——它是唯一的救生索。**

这就是**决策阈值距离判据**的第一形态：**定量精度的价值与样本离决策阈值的距离成反比——离得越远，越不值钱；离得越近，越救命。**

这个判据一口气解释了三个现象：

**现象一：Q8_0 和 FP16 的差异为什么可以忽略？** 因为生成章节的"定性判断"（过不过六检、连不连贯、出不出戏）远离决策阈值——绝大部分生成的 token 离"判死"的阈值都远得很，KV 量化带来的微小数值扰动根本推不动 sign。于是 Q8_0 省下的 412MB 显存是纯赚，精度损失是纯空耗 [6]。**不是 Q8_0 和 FP16 一样准，是这批样本离阈值太远，两者的差异够不到阈值。**

**现象二：验孕棒为什么有"灰色地带"？** 因为 hCG 浓度恰好在阈值附近的样本，定性判断必然不稳。这不是试剂盒做得不好，是任何二元坐标在阈值附近都有混沌区——这是离散化的代价，不是缺陷。医学上处理灰色地带的标准动作是复查（再测一次）、定量检测（测 hCG 浓度值）、或换更灵敏的阈值——全部是"把坐标从二元升级到连续"的动作，本质是**在阈值附近主动购买定量精度**。

**现象三：为什么"必须语义拆分"是废话而"拆成五类"不是？** 因为"语义拆分"没有定义阈值——没有划界，就没有阈值距离，连"离阈值远不远"都无从谈起。**死板废话的另一个结构性病因：不划界。** 划了界（拆成五类），才有阈值（这算不算第 3 类），才有距离感（这个样本明显是第 2 类，不用纠结）。不划界，一切都是混沌。

于是决策阈值距离判据升级为完整形态：**要不要为精度付费，先问两件事——离阈值有多远，以及，有没有阈值。** 没有阈值（指令没划界），精度无从谈起，先划界；有阈值但离得远，精度是空耗，省下；有阈值且贴得近，精度是救命钱，双倍投入。

到这里，论点一（定性 vs 定量）和论点二（死板废话、两道杠）就在同一个判据下合流了：**定性坐标的精准来自划界，定量坐标的价值来自阈值距离。** 定性不是定量的低配版，定量也不是定性的升级版——它们是同一套决策机制在不同坐标下的两种投影。选择哪种坐标，取决于离阈值多远。

还剩最后一个问题，也是最初触发这篇文章的问题：**离阈值远时，定量精度的空耗到底有多大？** 这是论点三，下一节用 structured-writer 引擎的真实迭代数据把它算出来。

## 五、定性精度的空耗：Q8_0 与 FP16 的实证

我的结构化写作引擎 structured-writer 从 3.0.0b11 到 3.1.0b5 经历了三十多次迭代，绝大多数是模型精度、量化、后端选型、窗口配置相关的调优。这段更新日志（CHANGELOG.md）无意中成了一本"定量精度空耗"的野外观察记录——它记录了每一次"为精度付费"的尝试，以及每一次"精度付费打水漂"的实证。这一节把这本记录摊开。

**实证一：KV 量化 Q8_0 vs FP16（3.0.0b13）。**

KV cache 是 Transformer 推理时的注意力缓存，每个 token 都存一份 key/value 向量，精度越高占用越大。当时我把 KV 从 FP16（16-bit 浮点）换成 Q8_0（8-bit 量化），实测结果：同一模型、同一上下文长度，FP16 的 KV 比 Q8_0 多占 412MB 显存；同时 flash_attn 路径下 Q8_0 完全可用，且部分 offload 场景加载成功、推理正常 [6]。也就是说：**精度降了一个数量级，功能零损失，显存净省 412MB。**

这 412MB 有没有换来任何"更准"？没有。章节的定性产出（情节、人物、连贯性）在两档精度下无法区分。但这里有一个更值得记录的细节：当时切换量化的动机根本不是"要更准"，而是"放不下"——35B 模型权重 19.7GB + KV 缓存膨胀 10-30GB（q8_0 ~80KB/token），把 66GB 机器击穿（3.0.0b21 的记录）[7]。**精度档位是被资源约束逼着降的，而不是被"唯一预测"的追求逼着升的。** 反过来说，如果当时有人迷信"FP16 更精准、必须保住精度"，引擎就直接 OOM 崩了——为不存在的收益付真实存在的成本，这就是空耗的定义。

**实证二：VMM 慢路径 vs 估算层数（3.0.0b14）。**

n_gpu_layers=-1（全 GPU 卸载）对放不下的模型（35B > 8GB 显存）不会失败——llama.cpp 用 VMM 把权重硬映射进显存，放不下就溢出到系统内存，实测加载要 200-269 秒，且溢出层实际仍在 CPU 算 [8]。代码里 layers_try 第一个就试 -1，于是 35B 必中慢路径。改成"放得下才试 -1，放不下直接用估算层数"之后，加载时间从 211 秒降到 38 秒 [8]。

这个实证的精妙之处在于：**-1 是一个看起来"最精确"的参数（全 GPU，一点 CPU 都不留），但它是一个死板参数——它不考虑模型放不放得下。** 而"估算层数"是一个"精确的范围"（估算放得下多少层，就卸载多少层），它反而更快、更稳。**参数的数字位数和参数的正确性毫无关系：-1 比 14 更简单，但 14 才接近真实阈值。** 这就是"精准的论断 vs 精确的范围"在工程里的直接对照——-1 是精准的论断（"全 GPU"），估算层数是精确的范围（"放得下几层算几层"），后者碾压前者。

**实证三：llama.cpp 8 t/s vs LM Studio 20+ t/s（3.1.0b1）。**

这是最戏剧性的一次。同一模型（Qwen3.5/3.6-35B-A3B），llama.cpp 直挂后端只有 8 t/s，换到 LM Studio 后端 20+ t/s，吞吐翻了两倍半 [9]。根因是 llama-cpp-python 官方 PyPI 最高 0.3.35（2025 年初的内核，已停更），对新的 MoE 架构没有专家并行/CUDA MoE kernel 优化；LM Studio 用的是最新 llama.cpp master [9]。

注意这里的关键：**两档吞吐生成的章节定性相同——都能过六检，都是可用的叙事文本。** 吞吐是定量指标（t/s），但它的价值不在精度，而在时间成本；精度（8-bit vs 16-bit）在这种场景下根本没进入决策。**当定量指标的变化不改变任何定性结论时，这个定量指标对你的决策就是透明的。** 这不是说 t/s 不重要——它决定你等 10 分钟还是 4 分钟——而是说，t/s 的价值来自"少等一会儿"这个定性体验，而不是来自"更准"这个定量幻觉。

**实证四：模型规模的定性跃迁（3.0.0b11）。**

Qwen2.5-3B 判不了"叙事目的/起承转合"这种抽象标准——实测把 22 条问题筛到 8 条，剩下的仍然是伪问题 [10]。换成 Qwen3-8B 之后才勉强能判。这是整本日志里最明确的一个信号：**定性能力的跃迁来自状态空间本身的扩大（模型规模、推理能力），而不是来自同一状态空间内的精度提升（位数、量化档）。** 3B 判不了"起承转合"，换成 8B 就能判个大概——这是格点数变了；而 8B 用 FP16 还是 Q8_0 跑，判断结果基本不变——这是格点内的读数精度变了，无关痛痒。

四个实证放在一起，画出一条清晰的线：

| 实证 | 精度/定量投入 | 定性产出变化 | 结论 |
|------|--------------|-------------|------|
| b13 KV 量化 | FP16 → Q8_0（精度降 8 倍，显存省 412MB） | 无 | 省下的显存是纯赚，损失的精度是纯空耗 |
| b14 VMM 慢路径 | -1（全 GPU）→ 估算层数 | 加载 211s → 38s | 死板参数（-1）vs 精确范围，后者碾压 |
| 3.1.0b1 后端切换 | 8 t/s → 20+ t/s（同精度） | 无（同过六检） | 吞吐的价值在时间成本，不在精度 |
| b11 模型升级 | 3B → 8B（规模） | 伪问题 22 → 8 之后才收敛 | 定性跃迁靠状态空间扩大，不靠位数 |

这条线指向一个反直觉但被反复验证的结论：**在"执行链不长、生成量大"的场景里，定量精度的边际收益趋近于零。** structured-writer 的写作链路：规划 → 写作 → 六检 → 修复，环节有限，每一步的输出协议固定——这是"执行链不长"；每一章生成几千上万 token，一本书几十章——这是"生成量大"。在这种场景下，FP16 和 Q8_0 的差异、小数点后第几位的高维坐标差异，全部淹没在 softmax 采样噪声里（09d 写过：LLM 的每个 token 都是从 exp(zi/T) 分布里抽样出来的 [5]）——**采样噪声本身就是一个巨大的"阈值距离"：任何小于采样噪声量级的精度投入，都离决策阈值远到够不着。**

把"唯一"这个词再钉一次。用户原话问：小数点后那几位带来的更精准高维坐标，是否能带来"唯一"的那个预测？答案是双重的：**第一，softmax 采样器在架构上就不存在"唯一预测"——同一 prompt 跑两次，token 分布不同，输出大概率不同，"唯一"在范畴上就不成立（09d 的 exp(zi/T) 公式决定了这一点 [5]）；第二，就算存在"更接近唯一"的精度提升（比如 FP16 比 Q8_0 的 logits 更"细"），这个提升的量级也远小于采样噪声——它移动的是分布中心，而采样噪声覆盖的范围比分布中心移动的幅度大得多。** 追求"唯一预测"是一个范畴错误，把它翻译成工程语言就是：**在采样器的世界里，精度的下限是采样噪声，精度的上限也是采样噪声——噪声之内的一切精度投入都是空耗。**

## 六、精度价值双判据：阈值距离 × 误差传播路径长度

到这一节，把前面所有论证收拢成两个判据。为什么要两个？因为"阈值距离"判据解决了"精度买不买得起"（投入是否够到阈值），但它没回答另一个问题：**就算买得起，买来的精度有没有机会被消耗？** 一个精度投入，如果它的误差根本不会沿着执行链传播，那它买来的"准"就永远躺在原地睡大觉——这同样是空耗，而且是最隐蔽的空耗。所以需要第二个判据。

**判据一：决策阈值距离。** 定量精度的价值与样本离决策阈值的距离成反比。离得远，精度是死信息；离得近，精度是命脉。这个判据决定"要不要为精度付费"。

**判据二：误差传播路径长度。** 误差沿执行链传播的步数决定精度的消耗率。链短，误差没有机会放大，精度买了也浪费；链长，误差逐级累积，精度是救命钱。这个判据决定"买了精度之后，它有没有用武之地"。

两个判据是乘数关系，不是加法关系。设精度价值 V，阈值距离因子 D（离阈值越近越大），传播路径因子 L（链越长越大）：

```
V ≈ D × L
```

四个象限：

| 象限 | 阈值距离 | 传播路径 | 精度投入策略 |
|------|---------|---------|-------------|
| ① | 远 | 短 | **零投入**。structured-writer 写作链路：章节离"判死"阈值远，KV 量化误差 6 检不传播 → Q8_0 白赚 412MB |
| ② | 远 | 长 | **低投入**。误差沿链传播但每步都远离阈值，累积也够不到阈值——只买"防累积到阈值"的最低保底精度 |
| ③ | 近 | 短 | **中投入**。贴阈值但链短，一次判断定生死——买够能分清阈值两侧的精度即可 |
| ④ | 近 | 长 | **高投入**。贴阈值且误差复利——每一步的精度都在救后面的命，双倍投入，不留余地 |

对照四个实证：b13（KV 量化）落在象限①——离阈值远，链短（单步生成），零投入正确；b14（层数估算）本质是象限①的另一个案例——-1 不是"多投入精度"而是"死板参数"，精确范围零成本修正；3.1.0b1（后端切换）落在象限①——吞吐不是精度投入，是路径更换；b11（模型升级）落在象限③——判定"起承转合"贴阈值（3B 判不了 = 在阈值上摇摆），链短（判定是单步决策），所以要换更大模型——**但它换的是状态空间（格点数），不是位数（格内精度）**，这正说明象限③的正确动作是"扩坐标"，不是"加密坐标"。

误差传播路径长度还有一个和 09d 的直接接续：09d 写过细碎处理的幻觉累积公式 P(至少一次幻觉) = 1 - (1-p)^N [5]——N 就是传播路径长度的原型：细碎修复链越长，幻觉累积越逼近 1。精度在这里的对应物是：**如果链上每一步都带一个微小误差 ε（比如 Q8_0 的量化误差），N 步之后的总误差约等于 Nε（随机游走，误差同向时线性累积）。** 链长 N 足够大时，哪怕每步 ε 小得可怜，Nε 也能捅到阈值。这就是"链短 × 量大"和"链长 × 量小"的根本区别：**前者精度空耗（N 小，ε 永远够不到阈值），后者精度救命（N 大，ε 会累积成质变）。**

回到 structured-writer：写作链（规划 → 写作 → 六检 → 修复）的 N 小——每一章最多十几步，每一步的协议固定，误差没有累积的舞台；所以 Q8_0 的空耗是数学必然。而如果把它换成 09d 的细碎修复场景——改 A → 验证 → 发现 B → 改 B → 验证 → 发现 C，N 可以无限大——那里的每一步精度都在对抗幻觉复利，任何精度投入都值得。**同一个引擎里的两种场景，对精度的需求差了一个数量级，而区分它们的唯一指标就是传播路径长度。**

最后把两个判据和"唯一预测的幻觉"钉死：追求"唯一预测"是在象限①（甚至所有象限）里追求一个不存在的目标——采样器的输出没有唯一解，精度投入的上限是采样噪声。**正确的问题不是"怎么更准"，而是"离阈值多近、链有多长"——这两个问题的答案决定精度值多少钱，而不是精度本身决定。** 定量神话的祛魅，不是否定定量的价值，而是否定"定量自动等于精准"这个未经检验的默认值：定量只有在阈值距离近、传播路径长的地方才配叫精准，其他地方它只是昂贵的装饰。

## 七、结语：精准是坐标的选择，不是位数的竞赛

回到开头那道两道杠。验孕棒的深浅有没有意义？有——但只在接近阈值时有意义。远离阈值时，深浅是死信息，二值化就是最经济的精准；贴近阈值时，深浅是命脉，必须把坐标从二元升级到连续。**同样的道理贯穿全文：精准从来不是定量的专利，定性有自己的精准形态——离散坐标的精准；而定量的价值从来不由数字位数决定，由两个判据决定——离决策阈值的距离，和误差传播路径的长度。**

把四个问题全部收口：

1. **定性是不是没有精度的权宜之计？** 不是。定性是离散化坐标，划界清晰时它就是精准的，且不携带读数误差。定性的精度问题是划界问题，不是读数问题。
2. **为什么"必须语义拆分"是死板废话？** 因为强调的语义密度为零——可枚举、可定位、可验证，三查全不过。死板废话产生于要求空洞，不是要求严苛。且死板废话是概率模型的默认输出模式，提示词工程的核心动作是给坐标，不是加语气。
3. **定性中藏不藏着定量？** 藏着。任何二元定性背后都有一条连续量纲和一道阈值。被丢掉的深浅信息，其价值取决于离阈值的距离——这是决策阈值距离判据。
4. **什么时候该为定量精度付费？** 两个判据相乘：离阈值近 × 路径长，双倍投入；离阈值远 × 路径短，零投入。structured-writer 三十余次迭代证明，写作链属于后者，Q8_0 空耗 FP16 的 412MB 是纯赚。

一条主线贯穿 09 系列：09 说穷举是宿命，09b 说精密度优先于正确度，09c 说匹配要让位给推理，09d 说粒度是地形，这一篇说的是——**精度也是地形：在阈值距离远、传播路径短的地形上，定量是装饰；在阈值距离近、传播路径长的地形上，定量是命脉。** 定量神话的祛魅不是反智，是把精度花在它该花的坐标上。**精准是坐标的选择，不是位数的竞赛。**

---

## 引用来源

> 引文全部来自本人项目的实际代码、实测数据与本地知识库（KB：大模型万象/政经文哲/τ scaling/LLM奠基理论），文献均为库内实有原文；[6][7][8][9][10][11] 为本人 structured-writer 引擎更新日志（CHANGELOG.md）实证，[12][13][14] 为本地 KB 原文检索。

| 编号 | 文献 | 出处 |
|------|------|------|
| [1] | Hinton, Vinyals & Dean. *Distilling the Knowledge in a Neural Network*（softmax-temperature：`pi = exp(zi/T) / Σj exp(zj/T)`，温度缩放输出分布） | KB「LLM奠基理论」（softmax-temperature 原文，FTS 检索实证） |
| [2] | OpenAI. *GPT-4 Technical Report*（"can suffer from 'hallucinations'...tendency to produce hallucinations"——幻觉是机制性倾向） | KB「LLM奠基理论」（OpenAI 2023 原文，FTS 检索实证） |
| [3] | wUwproject. *09c：天然不对等与噪音抑制——语义匹配范式的不对等困境与推理免疫*（推理免疫：自回归语义解码范式对形式差异免疫） | Cogito_Scribit 方法论体系（本人文章） |
| [4] | wUwproject. *09b：配置推动的穷举一致性——统一推动点位与误差的哲学*（精密度优先于正确度：统一点位→有限枚举→偏差一致→误差抵消/补正） | Cogito_Scribit 方法论体系（本人文章） |
| [5] | wUwproject. *09d：细碎处理与区域重构的成本切割——颗粒度与对齐的悖论*（softmax 采样噪声内禀、幻觉累积 1-(1-p)^N、五确定决策树、验证迷思） | Cogito_Scribit 方法论体系（本人文章） |
| [6] | wUwproject. structured-writer CHANGELOG 3.0.0b13：KV 量化 Q8_0 vs FP16 实测省 412MB，flash_attn 路径 Q8_0 完全可用；35B n_ctx=262K 层数估算 7 → 9 层 | 本人项目实证（structured-writer/CHANGELOG.md） |
| [7] | wUwproject. structured-writer CHANGELOG 3.0.0b21：修复引擎 35B 不释放 + OOM——KV cache 膨胀 10-30GB（q8_0 ~80KB/token）击穿 66GB 机器；修复专用 n_ctx 固定 16384 | 本人项目实证（structured-writer/CHANGELOG.md） |
| [8] | wUwproject. structured-writer CHANGELOG 3.0.0b14：n_gpu_layers=-1 触发 VMM 慢路径，35B 加载 211s → 38s（估算层数） | 本人项目实证（structured-writer/CHANGELOG.md） |
| [9] | wUwproject. structured-writer CHANGELOG 3.1.0b1：llama.cpp 直挂 8 t/s vs LM Studio 20+ t/s（llama-cpp-python 0.3.35 停更内核缺 MoE 优化），llama.cpp 直挂整体废弃 | 本人项目实证（structured-writer/CHANGELOG.md） |
| [10] | wUwproject. structured-writer CHANGELOG 3.0.0b11：Qwen2.5-3B 判不了"叙事目的/起承转合"抽象标准（22→8 条仍伪问题）→ 换 Qwen3-8B | 本人项目实证（structured-writer/CHANGELOG.md） |
| [11] | wUwproject. structured-writer CHANGELOG 3.0.0b22：用户定性指令"压根不是爆内存包缓存，查代码！！"——崩溃真因为并发竞态，与内存/KV 无关 | 本人项目实证（structured-writer/CHANGELOG.md） |
| [12] | *PEFT 综述*（arXiv 2504.14117）：Precision-Aware Quantization——量化是降低计算复杂度的基础技术，精度感知是量化设计的核心维度 | KB「大模型万象」（2504.14117v1 原文，FTS 检索实证） |
| [13] | 政经文哲库：《中国式现代化》相关文献（"二分法"逻辑：事实判断 vs 价值判断的二分框架；二分类变量 1=有/0=无 的测量化） | KB「政经文哲」（FTS 检索实证） |
| [14] | τ scaling 库：数字政府治理文献（LDA 主题模型对政策文本的语义抽取——定性操作的结构化落地） | KB「τ scaling」（FTS 检索实证） |
| [15] | wUwproject. *09：穷举的宿命——稳定与随机的悖论*（本文的数学母体：随机误差→被约束的确定性波动） | Cogito_Scribit 方法论体系（本人文章） |

> 说明：[12][13][14] 为对应 KB 的 FTS 关键词命中原文；"强调语义密度三查""决策阈值距离判据""误差传播路径长度判据""V ≈ D × L 四象限"为本文的数学推导，非文献原话；"Q8_0 空耗 412MB""VMM 慢路径 211s → 38s""llama.cpp 8 t/s vs LM Studio 20+ t/s""3B 判不了抽象标准"为 structured-writer CHANGELOG 实际记录。

---

## 延伸阅读

- **09：穷举的宿命——稳定与随机的悖论**：本文的数学母体。"唯一预测是范畴错误"直接承袭 09 的"稳定与随机"命题——采样器的随机性是内禀的，任何精度投入都无法消除。
- **09b：配置推动的穷举一致性**：本文第二节的直接先导。"精密度优先于正确度"是定量世界的策略；本文论证"何时该放弃精密度"——阈值距离远时，正确度的划界远比精密度的读数重要。
- **09d：细碎处理与区域重构的成本切割**：本文第六节的直接接续。09d 的幻觉累积公式 1-(1-p)^N 是"误差传播路径长度"判据的原型；本文把它推广到精度误差的累积。
- **structured-writer 引擎**：`CHANGELOG.md`（3.0.0b11~3.1.0b5，三十余次迭代的精度/量化/后端实测记录）——本文全部工程实证的来源。
- **skill-standardization 审计技能**：`scripts/skill_audit/`（五确定输出协议 + 30+ fix_* 确定性修复）——"定性是离散化坐标"的工程实证。

---

*最后更新：2026-08-18（09e：精准定性的边界篇。承接 09 穷举宿命、09b 精密度优先于正确度、09c 推理免疫、09d 颗粒度悖论，把 structured-writer 引擎 3.0.0b11~3.1.0b5 三十余次迭代中反复出现的"定量精度空耗"现象提炼为双判据框架。核心论证：定量不是精准的唯一形态——定性是离散化坐标（二元/多元/区间/连续谱系），定性的精度问题是划界问题而非读数问题，精准的对立面不是定性是混淆；死板废话 = 强调语义密度为零——指令侧三查（可枚举/可定位/可验证），死板废话产生于要求空洞而非要求严苛，且是概率模型的默认输出模式（b22 用户"查代码不是内存"实证）；定性中隐藏定量——任何二元定性背后都有一条连续量纲和一道阈值，被丢掉的深浅信息价值取决于离阈值距离；定性精度的空耗实证——KV 量化 Q8_0 省 412MB 定性无差（b13）、VMM 慢路径 211s→38s（b14）、llama.cpp 8 t/s vs LM Studio 20+ t/s（3.1.0b1）、3B 判不了抽象标准需换 8B（b11）；双判据：决策阈值距离 × 误差传播路径长度，V ≈ D × L 四象限，写作链属"远×短"零投入象限；唯一预测是范畴错误——采样器精度上下限都是采样噪声。文献佐证 15 条：KB 原文检索 3（大模型万象 1 / 政经文哲 1 / τ scaling 1）+ LLM奠基理论 2 + 本人项目实证 6 + 方法论体系自引 4。哲学命题：精准是坐标的选择，不是位数的竞赛；精度也是地形——阈值距离远、路径短的地形上定量是装饰，阈值距离近、路径长的地形上定量是命脉。)*
