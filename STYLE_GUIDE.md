# Cogito_Scribit 写作样式规范（STYLE GUIDE）

> 本文档固定本书/本系列的**表格**与**流程图**写作标准。
> 写文章时直接按此规范写 markdown，构建管线（md2html）会自动渲染成统一的 HTML/EPUB 样式。
> 标准一旦固定不再变动——改规范 = 改文档 + 改 md2html + 全量 rebuild。

---

## 一、表格规范

### 1.1 全局规则

| 规则 | 值 |
|---|---|
| 表格总宽度 | 一律 100%（与正文内容区等宽） |
| 单元格边框 | 统一细边框（渲染器固定） |
| 单元格内多信息 | 用 `<br>` 强制分行，**一行一个原子信息** |
| 中文折断 | 渲染器 `word-break: keep-all`，中文不拆词 |

### 1.2 表格分类与列宽（核心）

**① 引用编号索引表（表头 = `编号 | 文献 | 出处`）**

仅此一种表使用 `table-layout: fixed` 固定列宽，且**所有此类表列宽比例完全一致**：

| 列 | 固定宽度 | 说明 |
|---|---|---|
| 编号 | 8% | 容纳约 6 字符（`[22]` 足够） |
| 文献 | 47% | 论文标题 + 说明 |
| 出处 | 45% | KB 库名 / 文件名 / 类型注释 |

- 识别方式：md2html 检测表头第一列 = `编号` 且第二列 = `文献` 时自动加 `ref-table` 类，无需手工标注
- **写文章时：引用编号表必须用这 3 列表头，不要改列名**

**② 普通信息表（表头任意，非编号/文献/出处）**

- 使用 `table-layout: auto`，列宽由浏览器按内容自动分配
- 不手工指定列宽（百分比/px 都不要写）

**③ 列数约定**

- 表格列数 = 信息维度数，一列一个维度
- 3 列表、4 列表、5 列表均允许，但**同列数的表格列宽比例自动一致**（渲染器按列数统一分配）

### 1.3 单元格书写规则

```
| 编号 | 文献 | 出处 |
|------|------|------|
| [1] | 作者. 标题（年份） | KB「库名」<br>`文件名`<br>（论文原文，FTS 检索实证） |
```

- **一行一个原子信息**：KB 库名 / 文件名 / 类型注释三者用 `<br>` 分三行
- 括号内是一个完整信息时**不要拆**（如 `（联网核验，辅证）` 保持一行）
- 禁止在出处列塞论文标题/作者/出版信息（这些在文献列）

---

## 二、流程图规范

### 2.1 唯一推荐写法：代码块 + 单向箭头链

**简单单向链**（最常用）：

```markdown
```
正常执行 → 发生阻断 → 写入锁文件 → 进程退出(2)
    ↓
（外部修复问题）
    ↓
重新执行，传入恢复参数 → 读取锁文件 → 跳过已完成步骤 → 从阻断点继续
```
```

渲染：HTML 流程卡片流（每段一个卡片，`→` 后内容高亮，`↓` 为连接箭头）。

**规则**：
- 用三个反引号包裹（代码块）
- 横向连接用 `→`，竖向连接用 `↓`
- 步骤/状态用普通文本，分支说明用 `├─` / `└─` 前缀
- 阶段/大区段标题用 `【阶段名】`（渲染为蓝色粗体阶段条）
- 括号内是单个语义单元（如 `（外部修复问题）`）保持一行

### 2.2 复杂框图（阶段分层 + 嵌套）

```markdown
```
┌──────────────────────────────┐
│  【每步执行循环】            │
│  步骤N开始前：              │
│    检查上一步的状态          │
│    ├─ 已完成 → 继续         │
│    └─ 未REVIEW → 触发中断   │
│  执行步骤N                  │
└──────────────────────────────┘
    ↓
【恢复阶段】
外部读取锁文件 → 修复问题 → --continue 重跑
```
```

**规则**：
- 允许使用 ASCII 框线（`┌┐└┘│─┬┴`），渲染器自动识别并转 HTML 卡片流
- 框线字符会被自动剥离，只显示真实文本
- 缩进 = 层级（渲染为 margin-left 递进）
- 阶段用 `【】`，分支用 `├─`/`└─`，连接用 `↓`/`▼`

### 2.3 什么不是流程图（保持代码框）

以下情况**不要**用流程图写法，渲染为普通代码框（`<pre>`）：

| 类型 | 示例 | 渲染 |
|---|---|---|
| 目录树 | `├── .state.json`（无箭头） | pre |
| 代码示例 | `def foo(): ...` / JSON | pre |
| 配置文件 | `.gitignore` 等 | pre |

判断标准：**含 `↓↑→▼` 箭头 ≥2 行，或含框线字符，才是流程图**；否则是代码。

### 2.4 禁止事项

1. 不要用裸 `→` 写流程图而不进代码块（正文段落里的箭头链不会转 HTML 图）
2. 不要在代码块里手工排版对齐（渲染器自动处理缩进）
3. 不要用 HTML 手写图（md 源保持纯文本，渲染层统一转换）

---

## 三、一句话速查

| 场景 | 写法 |
|---|---|
| 引用编号表 | 表头固定 `编号\|文献\|出处`，出处列 `<br>` 分行 |
| 单向流程 | 代码块内 `A → B → C` + `↓` |
| 复杂流程 | 代码块内 `【阶段】` + `├─/└─` 分支 + 框线 |
| 目录树/代码 | 普通代码块（不写箭头） |
| 正文箭头链 | 不要裸写（不会转换） |

---

*规范版本：v1.0（2026-08-22 定稿，与 md2html.py 渲染规则一一对应）*
